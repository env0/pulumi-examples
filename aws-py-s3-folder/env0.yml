version: 1
deploy:
  steps:
    setupVariables:
      after:
        - /home/node/.pulumi/bin/pulumi version
        - pip3 install -r requirements.txt
    pulumiLogin:
      before:
        - pulumi version
